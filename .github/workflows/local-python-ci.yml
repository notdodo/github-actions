name: Python CI (local repository)
on:
  push:
    branches:
      - main
    paths:
      - auto-tagger/**
  pull_request:
    paths:
      - auto-tagger/**
      - .github/workflows/local-python-ci.yml

concurrency:
  group: ghas-python-${{ github.ref }}
  cancel-in-progress: true

jobs:
  python-ci:
    uses: notdodo/github-actions/.github/workflows/python-ci.yml@cabf073f973a7074895d52d9ba3b59d5b05241ee
    # python-ci-v0.0.0
    with:
      poetry-version: 1.8.2
      python-version: 3.11
      working-directory: auto-tagger
